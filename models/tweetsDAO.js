/*
let Tweet = require('./tweetsModel');


module.exports = class tweetsDAO {
  static getAllTweets(){
  	return Tweet.find()
  		.catch(() => error("err"));
  }

  static getTweetByLocation(location){
  	return Tweet.find({location: location})
  		.catch(() => error("err"));
  }


};
*/
